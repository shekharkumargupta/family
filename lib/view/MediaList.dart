import 'package:flutter/material.dart';

class MediaList extends StatelessWidget{

  @override
  Widget build(BuildContext context) {
    return Container(
      child: new Text("Media List"),
    );
  }

}